# uc001 健康检查

## 文档维护说明

| 字段 | 内容 |
|---|---|
| 服务名称 | AI 投资人格与持仓适配分析系统 |
| 用例编号 | uc001 |
| 用例名称 | 健康检查 |
| 维护人员 | Lei Jianqiu |
| 生成日期 | 2026-05-08 |
| 代码来源 | `investment_analysis/api.py` |

## 1. 用例概述

### 1.1 业务目标

为部署、启动脚本或调用方提供轻量存活探测，确认标准库 HTTP 服务进程可以接收请求并返回 JSON 响应。

### 1.2 触发入口

| 类型 | 方法 | 路径/函数 | 代码位置 |
|---|---|---|---|
| HTTP | GET | `/health` | `InvestmentAnalysisHandler.do_GET` |

### 1.3 参与者

| 参与者 | 职责 |
|---|---|
| 调用方 | 发起健康检查请求 |
| 服务端 | 返回固定健康状态 |

## 2. 输入输出

### 2.1 请求输入

无请求体。

### 2.2 响应输出

| 字段 | 类型 | 说明 |
|---|---|---|
| status | string | 固定为 `ok` |

## 3. 主流程

```text
1. 服务端接收 GET /health。
2. 路由分发命中健康检查分支。
3. 返回 {"status": "ok"}，HTTP 状态码为 200。
```

## 4. 业务规则

| 规则 | 说明 |
|---|---|
| 无状态 | 不读取数据库，不依赖业务数据 |
| 固定响应 | 服务可达时始终返回 `status=ok` |

## 5. 异常流程

| 场景 | 触发条件 | 当前处理 |
|---|---|---|
| 路径不匹配 | 请求路径不是 `/health` | 由通用路由返回 404 |

## 6. 数据与状态

该用例不产生持久化数据。

## 7. 关联代码

| 文件 | 说明 |
|---|---|
| `investment_analysis/api.py` | HTTP 路由分发与 JSON 响应 |

## 8. 验收标准

1. `GET /health` 返回 HTTP 200。
2. 响应体为 JSON，且 `status` 等于 `ok`。
